name: UAT - Deploy

on: workflow_dispatch

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: explorer-web
  EVENT_NAME: uat-explorer-web
  VALUES_FILE: core/common-values/explorer-web-values.yaml

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment: uat

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          role-to-assume: arn:aws:iam::114810186946:role/githubactions
          role-session-name: Github
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build, tag, and push image to Amazon ECR
        id: build-image
        env:
          API_KEY: ${{ secrets.UAT_API_KEY }}
          PRIVY_APP_ID: ${{ secrets.PRIVY_APP_ID }}
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker buildx build --platform=linux/amd64 -t  $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG  --build-arg ENV=uat --build-arg PRIVY_APP_ID=$PRIVY_APP_ID --build-arg API_KEY=$API_KEY .

          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          echo "::set-output name=image::$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"

      - name: Repository Dispatch
        env:
          IMAGE_TAG: ${{ github.sha }}
          EVENT_NAME: ${{ env.EVENT_NAME }}
          VALUES_FILE: ${{ env.VALUES_FILE }}
        run: |
          curl -H "Accept: application/vnd.github.everest-preview+json" \
          -H "Authorization: token ${{ secrets.ACCESS_TOKEN_PWD }}" \
          --request POST \
          --data '{"event_type": "'"$EVENT_NAME"'", "client_payload": {"name": "'"$EVENT_NAME"'", "file_path": "'"$VALUES_FILE"'", "tag": "'"$IMAGE_TAG"'"}}' \
          https://api.github.com/repos/Airstack-xyz/infra-uat-deployment/dispatches
